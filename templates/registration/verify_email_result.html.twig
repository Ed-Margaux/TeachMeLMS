{% extends 'base.html.twig' %}

{% block title %}Email Verification — Teach Me{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {% include 'partials/_public_header_styles.html.twig' %}
    <link rel="stylesheet" href="{{ asset('styles/public/public-footer.css') }}">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: #2c2c2c;
            line-height: 1.6;
            background: rgba(255, 197, 224, 0.18);
            margin: 0;
        }

        .wrap {
            max-width: 900px;
            margin: 0 auto;
            padding: 80px 24px;
        }

        .card {
            background: #fff;
            border: 1px solid rgba(0,0,0,0.06);
            border-radius: 18px;
            padding: 34px 26px;
            box-shadow: 0 8px 24px rgba(0,0,0,0.06);
        }

        .btn {
            display: inline-block;
            margin-top: 16px;
            padding: 12px 18px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            color: #fff;
            background: linear-gradient(135deg, #C53678, #ED64A6);
        }

        .btn:hover {
            background: linear-gradient(135deg, #B02D6B, #D8559A);
        }

        .muted { color: #666; }
    </style>
{% endblock %}

{% block body %}
<div class="public-page-with-footer">
    <div class="public-page-with-footer__main">
        <div class="wrap">
            <div class="card">
                {% if success %}
                    <h1 style="margin:0 0 10px 0;">Email verified</h1>
                    <p class="muted" style="margin:0;">Your account is now verified. You can sign in and continue.</p>
                {% else %}
                    <h1 style="margin:0 0 10px 0;">Verification link is invalid or expired</h1>
                    <p class="muted" style="margin:0;">Please register again or contact support if you need help.</p>
                {% endif %}

                <a class="btn" href="{{ path('app_login') }}">Go to login</a>
            </div>
        </div>
    </div>
    {% include 'partials/_public_footer.html.twig' %}
</div>
{% endblock %}

