# Stateless CSRF is for cacheable forms (submit). Login uses session CSRF (see prod session + index.php HTTPS).
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        check_header: false
        stateless_token_ids:
            - submit
