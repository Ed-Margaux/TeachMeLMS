# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            # Login on Railway: session cookies are unreliable behind php -S; use cookie-based CSRF.
            - authenticate
            - logout
