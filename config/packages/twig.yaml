twig:
    file_name_pattern: '*.twig'
    globals:
        # Resolve at runtime (not from cached parameters) so Railway / .env.prod.local values apply.
        google_oauth_client_id: '%env(string:default::GOOGLE_OAUTH_CLIENT_ID)%'

when@test:
    twig:
        strict_variables: true
