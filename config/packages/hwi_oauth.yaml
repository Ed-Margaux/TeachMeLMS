hwi_oauth:
    firewall_names: [main]

    resource_owners:
        google:
            type: google
            client_id: '%env(default::GOOGLE_OAUTH_CLIENT_ID)%'
            client_secret: '%env(default::GOOGLE_OAUTH_CLIENT_SECRET)%'
            scope: "email profile"